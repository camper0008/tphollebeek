{
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "sass": "^1.47.0"
  },
  "scripts": {
    "watch:sass": "sass --watch --update --style=expanded src/scss:public/css",
    "build:sass": "sass --no-source-map --style=compressed src/scss:public/css",
    "watch:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "build": "yarn build:sass && yarn build:eleventy"
  },
  "name": "tphollebeek",
  "version": "1.0.0",
  "description": "My website.",
  "repository": "https://github.com/camper0008/tphollebeek.git",
  "author": "piet <spetakel123@gmail.com>",
  "license": "GPL-3.0-only"
}
